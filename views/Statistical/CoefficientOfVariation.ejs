<!DOCTYPE html>
<html lang="en">

<head>
     <%- include('../Partials/Head'); %>
</head>

    <%- include('../Partials/Navbar'); %>
    <%- include('../Partials/Mode'); %>

<body class="bg-gradient-to-br from-blue-50 via-white to-purple-100 min-h-screen px-6 py-16">

<div class="max-w-7xl mx-auto">

    <!-- Heading -->
    <div class="text-center mb-10">

        <h1 class="text-4xl md:text-5xl font-extrabold text-gray-800">
            Coefficient of Variation
        </h1>

        <p class="mt-3 text-lg text-gray-600">
            Enter Xi and Fi values
        </p>

    </div>

    <!-- Input Table -->
    <div class="bg-white shadow-xl rounded-3xl overflow-hidden">

        <table class="w-full text-center">

            <thead class="bg-gray-100 text-lg text-gray-700">

                <tr>

                    <th class="border py-4">Xi</th>

                    <th class="border py-4">Fi</th>

                </tr>

            </thead>

            <tbody id="table-body">

                <tr>

                    <td class="border p-4">
                        <input type="number"
                            class="w-28 px-3 py-2 border rounded-xl text-center"
                            placeholder="Xi">
                    </td>

                    <td class="border p-4">
                        <input type="number"
                            class="w-28 px-3 py-2 border rounded-xl text-center"
                            placeholder="Fi">
                    </td>

                </tr>

            </tbody>

        </table>

    </div>

    <!-- Buttons -->
    <div class="flex justify-center gap-4 mt-10">

        <button onclick="addRow()"
            class="px-6 py-3 bg-blue-200 rounded-2xl font-bold hover:bg-blue-300 transition">

            ➕ Add Row

        </button>

        <button onclick="calculate()"
            class="px-6 py-3 bg-green-200 rounded-2xl font-bold hover:bg-green-300 transition">

            📊 Calculate

        </button>

         <a href="/Statistical" class="px-6 py-3 text-lg font-semibold text-black bg-pink-200 rounded-2xl shadow-md
         hover:bg-pink-300 hover:scale-105 hover:shadow-lg
         transition duration-300">
                            👈 Back
                    </a>

    </div>

    <!-- Result -->
    <div id="result-section" class="hidden mt-14">

        <h2 class="text-3xl font-bold text-center mb-6">

            Calculation Table

        </h2>

        <div class="overflow-x-auto bg-white shadow-lg rounded-2xl">

            <table class="w-full text-center border">

                <thead class="bg-gray-100">

                    <tr>

                        <th class="border p-3">Xi</th>

                        <th class="border p-3">Fi</th>

                        <th class="border p-3">FiXi</th>

                        <th class="border p-3">Xi - X̄</th>

                        <th class="border p-3">(Xi - X̄)²</th>

                        <th class="border p-3">Fi(Xi - X̄)²</th>

                    </tr>

                </thead>

                <tbody id="result-body"></tbody>

            </table>

        </div>

        <!-- Formula -->
        <div id="formula"
            class="mt-8 bg-blue-50 p-8 rounded-2xl shadow-md text-gray-800">

        </div>

    </div>

</div>

<script>

// ➕ Add Row
function addRow() {

    const table = document.getElementById("table-body");

    const row = `

    <tr>

        <td class="border p-4">
            <input type="number"
                class="w-28 px-3 py-2 border rounded-xl text-center"
                placeholder="Xi">
        </td>

        <td class="border p-4">
            <input type="number"
                class="w-28 px-3 py-2 border rounded-xl text-center"
                placeholder="Fi">
        </td>

    </tr>

    `;

    table.insertAdjacentHTML("beforeend", row);

}



// 📊 Calculate
function calculate() {

    const rows = document.querySelectorAll("#table-body tr");

    const resultBody = document.getElementById("result-body");

    const formulaDiv = document.getElementById("formula");

    resultBody.innerHTML = "";

    formulaDiv.innerHTML = "";

    let data = [];

    let totalFi = 0;

    let totalFixi = 0;

    // 🔹 Collect Data
    rows.forEach(row => {

        const inputs = row.querySelectorAll("input");

        const xi = parseFloat(inputs[0].value);

        const fi = parseFloat(inputs[1].value);

        if (isNaN(xi) || isNaN(fi)) return;

        const fixi = xi * fi;

        totalFi += fi;

        totalFixi += fixi;

        data.push({
            xi,
            fi,
            fixi
        });

    });

    // ❌ Validation
    if (data.length === 0) {

        alert("Please enter valid data!");

        return;
    }

    // 🔹 Mean
    const mean = totalFixi / totalFi;

    let totalFiSquare = 0;

    // 🔹 Build Table
    data.forEach(d => {

        const minusMean = d.xi - mean;

        const square = minusMean * minusMean;

        const fiSquare = d.fi * square;

        totalFiSquare += fiSquare;

        resultBody.innerHTML += `

        <tr>

            <td class="border p-3">
                ${d.xi}
            </td>

            <td class="border p-3">
                ${d.fi}
            </td>

            <td class="border p-3">
                ${d.fixi.toFixed(2)}
            </td>

            <td class="border p-3">
                ${minusMean.toFixed(2)}
            </td>

            <td class="border p-3">
                ${square.toFixed(2)}
            </td>

            <td class="border p-3">
                ${fiSquare.toFixed(2)}
            </td>

        </tr>

        `;

    });

    // 🔹 Total Row
    resultBody.innerHTML += `

    <tr class="bg-gray-100 font-bold">

        <td class="border p-3"></td>

        <td class="border p-3">
            N = ${totalFi}
        </td>

        <td class="border p-3">
            ΣFiXi = ${totalFixi.toFixed(2)}
        </td>

        <td class="border p-3"></td>

        <td class="border p-3"></td>

        <td class="border p-3">
            ΣFi(Xi-X̄)² = ${totalFiSquare.toFixed(2)}
        </td>

    </tr>

    `;

    // 🔹 Variance
    const variance = totalFiSquare / totalFi;

    // 🔹 Standard Deviation
    const sd = Math.sqrt(variance);

    // 🔹 Coefficient of Variation
    const cv = (sd / mean) * 100;

    // 🔹 Formula Section
    formulaDiv.innerHTML = `

    <div>

        <h2 class="text-3xl font-bold text-center mb-8">

            Step-by-Step Solution

        </h2>

        <div class="space-y-5 text-lg">

            <p>
                <b>Step 1:</b> Mean
            </p>

            <p>
                Mean = ΣFiXi / N
            </p>

            <p>
                = ${totalFixi.toFixed(2)} / ${totalFi}
            </p>

            <p class="font-bold text-blue-600">
                Mean = ${mean.toFixed(2)}
            </p>

            <hr>

            <p>
                <b>Step 2:</b> Standard Deviation
            </p>

            <p>
                SD = √( ΣFi(Xi-X̄)² / N )
            </p>

            <p>
                = √( ${totalFiSquare.toFixed(2)} / ${totalFi} )
            </p>

            <p>
                = √(${variance.toFixed(2)})
            </p>

            <p class="font-bold text-green-600">
                SD = ${sd.toFixed(2)}
            </p>

            <hr>

            <p>
                <b>Step 3:</b> Coefficient of Variation
            </p>

            <p>
                CV = (SD / Mean) × 100
            </p>

            <p>
                = (${sd.toFixed(2)} / ${mean.toFixed(2)}) × 100
            </p>

            <p class="text-3xl font-bold text-purple-600">
                CV = ${cv.toFixed(2)}%
            </p>

        </div>

    </div>

    `;

    // 🔹 Show Result
    document.getElementById("result-section")
        .classList.remove("hidden");

}

</script>

</body>
</html>